# Google Gemini API key — free tier at https://aistudio.google.com/apikey
# Optional: the app runs fully without it, falling back to precomputed review
# summaries in data/summaries.fallback.json (RULES.md D3).
GEMINI_API_KEY=
